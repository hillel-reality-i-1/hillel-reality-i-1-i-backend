# this is for API Places from google for choose Country/City

# import googlemaps
# from django.conf import settings
#
#
# def get_places(api_key, input_text):
#     gmaps = googlemaps.Client(key=api_key)
#     places_result = gmaps.places_autocomplete(input_text)
#     return places_result
